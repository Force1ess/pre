#include<iostream>
#include<map>
#include<queue>
#include<algorithm>
#include<cstring>
#define rep(i, a, b) for (int i = (a); i < (b); ++i)
#define per(i, a, b) for (int i = a; i > (b); --i)
#include<vector>
typedef pair<int,int> P;
typedef long long ll;
//fill��ʼ����Ԫ����fill(a[0],a[0]+maxn*maxv,inf); 
//
#include <iostream>
#include <cstdio>
#include <fstream>
#include <algorithm>
#include <cmath>
#include <iomanip>
#include <deque>
#include <vector>
#include <queue>
#include <string>
#include <cstring>
#include <map>
#include <stack>
#include <set>
#include <sstream>
#define IOS                       \
	ios_base::sync_with_stdio(0); \
	cin.tie(0);
#define mod 1000000007
#define eps 1e-6
#define ll long long
#define INF 0x3f3f3f3f
#define MEM(t, y) memset(t, y, sizeof(t))
#define pb push_back
#define mk make_pair
#define pi pair<int, int>
#define rep(i, a, b) for (int i = (a); i <= (b); ++i)
#define per(i, a, b) for (int i = a; i >= (b); --i)
using namespace std;
#define *(i) cout<<i<<"*"<<endl
#define ***(i,j) cout<<i<<"***"<<j<<endl 
int dt[][2] = {{1, 0}, {-1, 0}, {0, 1}, {0, -1}, {0, 0}};
//typedef pair<int, int> P;
//priority_queue<int, vector<int>, greater<int> > q;
const int N = 1e7 + 5;
const int M = 5e5 + 5;
